#pragma once

/**
 * Contains random config stuff that is too general to go somewhere else.
 */
namespace Config {
    float const MAX_BUFFER_SIZE = 256;
    float const SAMPLE_RATE = 44100.0f;
    unsigned int const MAX_DSPS = 512;
};
